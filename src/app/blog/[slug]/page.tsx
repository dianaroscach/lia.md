import type {Metadata} from "next";import Image from "next/image";import {notFound} from "next/navigation";import {articles} from "@/data/articles";import {ArticleContent} from "@/components/blog/ArticleContent";import {ArticleCard} from "@/components/blog/ArticleCard";import {Container} from "@/components/layout/Container";import {Button} from "@/components/ui/Button";
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,a=articles.find(x=>x.slug===slug);return a?{title:a.title,description:a.excerpt,openGraph:{title:a.title,description:a.excerpt,images:[a.image]}}:{}};
export default async function ArticlePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params,a=articles.find(x=>x.slug===slug);if(!a)notFound();const related=articles.filter(x=>x.slug!==slug).slice(0,2);return <Container className="py-16"><article><header className="mx-auto max-w-3xl text-center"><p className="text-xs font-bold uppercase tracking-[.2em] text-[var(--wine)]">{a.category} · {a.date} · {a.readTime}</p><h1 className="mt-5 text-5xl md:text-6xl">{a.title}</h1><p className="mt-6 text-xl leading-8 text-[var(--muted)]">{a.excerpt}</p></header><div className="relative mx-auto mt-12 aspect-[16/8] max-w-5xl overflow-hidden rounded-[2rem]"><Image src={a.image} alt="Ilustrație pentru articol" fill sizes="(max-width: 768px) 100vw, 50vw" priority className="object-cover"/></div><div className="mx-auto mt-12 max-w-2xl"><ArticleContent article={a}/><aside className="mt-12 rounded-3xl bg-[var(--paper)] p-7"><h2 className="text-2xl">Ai o întrebare despre propria ie?</h2><p className="my-4 text-[var(--muted)]">O consultație individuală poate transforma observațiile într-un plan clar.</p><Button href="/consultatii">Solicită o consultație</Button></aside></div></article><section className="mt-24"><h2 className="text-3xl">Articole similare</h2><div className="mt-8 grid gap-8 md:grid-cols-2">{related.map(x=><ArticleCard key={x.slug} article={x}/>)}</div></section><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({"@context":"https://schema.org","@type":"Article",headline:a.title,datePublished:"2026-05-12",author:{"@type":"Person",name:"Autoarea Ciocârlia (placeholder)"}})}}/></Container>}
