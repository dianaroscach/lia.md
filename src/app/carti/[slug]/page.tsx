import type {Metadata} from "next";import {notFound} from "next/navigation";import {books} from "@/data/books";import {Container} from "@/components/layout/Container";import {BookGallery} from "@/components/books/BookGallery";import {BookPurchaseBox} from "@/components/books/BookPurchaseBox";import {BookDetails} from "@/components/books/BookDetails";import {Accordion} from "@/components/ui/Accordion";import {BookCard} from "@/components/books/BookCard";
export function generateStaticParams(){return books.map(b=>({slug:b.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,b=books.find(x=>x.slug===slug);return b?{title:b.title,description:b.description,openGraph:{title:b.title,description:b.description,images:[b.image]}}:{}};
export default async function BookPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params,book=books.find(x=>x.slug===slug);if(!book)notFound();const other=books.find(x=>x.id!==book.id);return <Container className="py-16"><div className="grid gap-12 lg:grid-cols-2"><BookGallery image={book.image} title={book.title}/><div><p className="text-xs font-bold uppercase tracking-[.2em] text-[var(--wine)]">Biblioteca Ciocârlia</p><h1 className="mt-4 text-5xl">{book.title}</h1><p className="mt-3 text-xl italic text-[var(--muted)]">{book.subtitle}</p><p className="mt-7 text-lg leading-8">{book.description}</p><p className="mt-5 leading-7 text-[var(--muted)]">Pentru cititorii curioși, meșterițe la început de drum și familii care vor să înțeleagă mai bine piesele moștenite.</p><div className="mt-8"><BookPurchaseBox book={book}/></div></div></div><section className="py-20"><BookDetails book={book}/></section><section className="border-t border-black/10 py-16"><h2 className="text-3xl">Întrebări frecvente</h2><Accordion items={[{question:"Pot comanda din afara Republicii Moldova?",answer:"În MVP, livrarea internațională se confirmă individual. Integrarea automată va veni odată cu ecommerce-ul."},{question:"Este potrivită pentru începători?",answer:"Da. Noțiunile sunt explicate progresiv, iar termenii specializați au context."},{question:"Cum se livrează?",answer:"Cartea este protejată pentru transport și expediată prin serviciul stabilit la confirmarea comenzii."}]}/></section>{other&&<section><h2 className="mb-8 text-3xl">Continuă lectura</h2><BookCard book={other}/></section>}<script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({"@context":"https://schema.org","@type":"Book",name:book.title,description:book.description,offers:{"@type":"Offer",price:book.price,priceCurrency:"MDL",availability:"https://schema.org/InStock"}})}}/></Container>}
