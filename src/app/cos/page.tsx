"use client";import {useCart} from "@/hooks/useCart";import {CartItem} from "@/components/shop/CartItem";import {CartSummary} from "@/components/shop/CartSummary";import {Container} from "@/components/layout/Container";import {Button} from "@/components/ui/Button";
export default function Cart(){const {items,change,remove,subtotal}=useCart();return <Container className="py-20"><h1 className="text-5xl">Coșul tău</h1><p className="mt-3 text-[var(--muted)]">Produsele sunt păstrate pe acest dispozitiv.</p><div className="mt-10 grid gap-10 lg:grid-cols-[1fr_360px]"><div>{items.length?items.map(x=><CartItem key={x.product.id} line={x} onChange={n=>change(x.product.id,n)} onRemove={()=>remove(x.product.id)}/>):<div className="rounded-3xl border border-dashed border-black/20 p-10 text-center"><p className="text-xl">Coșul este încă gol.</p><Button href="/magazin" className="mt-5">Descoperă magazinul</Button></div>}</div><CartSummary subtotal={subtotal}/></div></Container>}
