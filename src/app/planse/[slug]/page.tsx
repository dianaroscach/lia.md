import type {Metadata} from "next";import Image from "next/image";import {notFound} from "next/navigation";import {patterns} from "@/data/patterns";import {Container} from "@/components/layout/Container";import {PatternDetails} from "@/components/patterns/PatternDetails";import {PatternOrderForm} from "@/components/patterns/PatternOrderForm";import {formatCurrency} from "@/lib/formatCurrency";
export function generateStaticParams(){return patterns.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params,p=patterns.find(x=>x.slug===slug);return p?{title:p.title,description:p.description,openGraph:{title:p.title,description:p.description,images:[p.image]}}:{}};
export default async function PatternPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params,p=patterns.find(x=>x.slug===slug);if(!p)notFound();return <Container className="py-16"><div className="grid gap-12 lg:grid-cols-2"><div className="relative aspect-square overflow-hidden rounded-[2rem]"><Image src={p.image} alt={p.title} fill sizes="(max-width: 768px) 100vw, 50vw" className="object-cover"/></div><div><p className="text-xs font-bold uppercase tracking-[.2em] text-[var(--wine)]">Planșă de cusut</p><h1 className="mt-4 text-5xl">{p.title}</h1><p className="mt-6 text-lg leading-8 text-[var(--muted)]">{p.description}</p><div className="my-8"><PatternDetails pattern={p}/></div><p className="mb-5 text-2xl font-bold">de la {formatCurrency(p.price)} <small className="font-normal">· demonstrativ</small></p><PatternOrderForm/></div></div><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify({"@context":"https://schema.org","@type":"Product",name:p.title,description:p.description,offers:{"@type":"Offer",price:p.price,priceCurrency:"MDL"}})}}/></Container>}
