import type {Product} from "@/types/product";import {formatCurrency} from "@/lib/formatCurrency";import {AddToCartButton} from "./AddToCartButton";
export function ProductCard({product}:{product:Product}){return <article className="flex flex-col rounded-3xl border border-black/10 bg-[var(--paper)] p-6"><p className="text-xs font-bold uppercase tracking-wider text-[var(--wine)]">{product.type}</p><h3 className="mt-3 text-2xl">{product.title}</h3><p className="mt-3 grow leading-7 text-[var(--muted)]">{product.description}</p><p className="my-5 font-bold">{formatCurrency(product.price)} <small className="font-normal">· demonstrativ</small></p><AddToCartButton product={product}/></article>}
