"use client";import type {CartLine} from "@/hooks/useCart";import {formatCurrency} from "@/lib/formatCurrency";
export function CartItem({line,onChange,onRemove}:{line:CartLine;onChange:(n:number)=>void;onRemove:()=>void}){return <div className="grid gap-4 border-b border-black/10 py-6 sm:grid-cols-[1fr_auto_auto] sm:items-center"><div><p className="text-xs uppercase tracking-wider text-[var(--wine)]">{line.product.type}</p><h3 className="text-xl">{line.product.title}</h3><p className="text-sm text-[var(--muted)]">{formatCurrency(line.product.price)} / buc.</p></div><label className="text-sm">Cantitate <input className="ml-2 w-16 rounded-lg border p-2" type="number" min="1" value={line.quantity} onChange={e=>onChange(Number(e.target.value))}/></label><div className="sm:text-right"><p className="font-bold">{formatCurrency(line.product.price*line.quantity)}</p><button onClick={onRemove} className="mt-2 text-sm text-[var(--wine)] underline">Șterge</button></div></div>}
