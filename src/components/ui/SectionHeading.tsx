export function SectionHeading({eyebrow,title,intro,align="left"}:{eyebrow?:string;title:string;intro?:string;align?:"left"|"center"}){return <div className={`max-w-2xl ${align==="center"?"mx-auto text-center":""}`}>{eyebrow&&<p className="mb-3 text-xs font-bold uppercase tracking-[.2em] text-[var(--wine)]">{eyebrow}</p>}<h2 className="text-4xl leading-tight md:text-5xl">{title}</h2>{intro&&<p className="mt-5 text-lg leading-8 text-[var(--muted)]">{intro}</p>}</div>}
