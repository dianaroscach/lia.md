import Image from "next/image";import Link from "next/link";import type {Book} from "@/types/book";import {formatCurrency} from "@/lib/formatCurrency";import {AddToCartButton} from "@/components/shop/AddToCartButton";import {Badge} from "@/components/ui/Badge";
export function BookCard({book}:{book:Book}){return <article className="grid overflow-hidden rounded-[1.75rem] border border-black/10 bg-[var(--paper)] sm:grid-cols-[.9fr_1.1fr]"><div className="relative min-h-72 bg-[var(--sand)]"><Image src={book.image} alt={`Coperta ${book.title}`} fill sizes="(max-width: 768px) 100vw, 50vw" className="object-cover"/></div><div className="flex flex-col p-7"><Badge>{book.availability}</Badge><h3 className="mt-4 text-3xl">{book.title}</h3><p className="mt-2 text-sm italic text-[var(--muted)]">{book.subtitle}</p><p className="mt-4 grow leading-7 text-[var(--muted)]">{book.description}</p><div className="mt-6 flex items-center justify-between gap-3"><div><strong className="text-xl">{formatCurrency(book.price)}</strong><small className="block text-[10px] uppercase tracking-wider">preț demonstrativ</small></div><div className="flex flex-wrap justify-end gap-2"><Link className="rounded-full border border-[var(--wine)] px-4 py-2 text-sm font-bold text-[var(--wine)]" href={`/carti/${book.slug}`}>Vezi cartea</Link><AddToCartButton product={book}/></div></div></div></article>}
