import type {Book} from "@/types/book";import {formatCurrency} from "@/lib/formatCurrency";import {AddToCartButton} from "@/components/shop/AddToCartButton";
export function BookPurchaseBox({book}:{book:Book}){return <div className="rounded-3xl bg-[var(--paper)] p-6 shadow-sm"><p className="text-3xl font-bold">{formatCurrency(book.price)}</p><p className="mb-5 text-xs text-[var(--muted)]">Preț demonstrativ · TVA inclus</p><AddToCartButton product={book} className="w-full"/><p className="mt-4 text-sm leading-6 text-[var(--muted)]">Livrare estimată în 2–4 zile lucrătoare în Republica Moldova. Detaliile finale se confirmă înainte de plată.</p></div>}
