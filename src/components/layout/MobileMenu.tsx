"use client";
import Link from "next/link";import {useState} from "react";import {navigation} from "@/data/navigation";
export function MobileMenu(){const [open,setOpen]=useState(false);return <div className="lg:hidden"><button aria-expanded={open} aria-controls="mobile-nav" onClick={()=>setOpen(!open)} className="border border-[var(--line)] px-4 py-2 text-xs uppercase tracking-[.12em]">{open?"Închide":"Meniu"}</button>{open&&<nav id="mobile-nav" className="absolute inset-x-0 top-full border-y border-[var(--line)] bg-[var(--ivory)] p-5 shadow-xl"><div className="flex flex-col">{navigation.map(x=><Link onClick={()=>setOpen(false)} className="border-b border-[var(--line)] py-4 font-serif text-lg" key={x.href} href={x.href}>{x.label}</Link>)}<Link onClick={()=>setOpen(false)} className="pt-4 font-serif text-lg text-[var(--wine)]" href="/cos">Coș</Link></div></nav>}</div>}
