import Image from "next/image";import Link from "next/link";import type {Pattern} from "@/types/pattern";import {formatCurrency} from "@/lib/formatCurrency";
export function PatternCard({pattern}:{pattern:Pattern}){return <article className="group overflow-hidden rounded-3xl border border-black/10 bg-[var(--paper)]"><Link href={`/planse/${pattern.slug}`}><div className="relative aspect-[4/3] overflow-hidden bg-[var(--sand)]"><Image src={pattern.image} alt={pattern.title} fill sizes="(max-width: 768px) 100vw, 50vw" className="object-cover transition duration-500 group-hover:scale-105"/></div><div className="p-6"><p className="text-xs font-bold uppercase tracking-wider text-[var(--wine)]">{pattern.region} · {pattern.difficulty}</p><h3 className="mt-2 text-2xl">{pattern.title}</h3><p className="mt-3 text-sm text-[var(--muted)]">{pattern.format}</p><p className="mt-5 font-bold">de la {formatCurrency(pattern.price)} <small className="font-normal">· demonstrativ</small></p></div></Link></article>}
