"use client";import {FormEvent,useState} from "react";import {Button} from "@/components/ui/Button";import {Input} from "@/components/ui/Input";export function PatternOrderForm(){const [done,setDone]=useState(false);return <form onSubmit={(e:FormEvent)=>{e.preventDefault();setDone(true)}} className="rounded-3xl bg-[var(--paper)] p-6"><label>Email pentru confirmare<Input type="email" required/></label><label className="mt-4 block">Opțiunea dorită<select className="field"><option>Format fizic</option><option>Format digital</option><option>Personalizare</option></select></label><Button className="mt-5 w-full">Solicită comanda</Button>{done&&<p className="mt-3 text-sm font-bold text-[var(--forest)]">Solicitarea demonstrativă a fost înregistrată.</p>}</form>}
