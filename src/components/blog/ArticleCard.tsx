import Image from "next/image";import Link from "next/link";import type {Article} from "@/types/article";
export function ArticleCard({article}:{article:Article}){return <article className="group"><Link href={`/blog/${article.slug}`}><div className="relative aspect-[16/10] overflow-hidden rounded-3xl bg-[var(--sand)]"><Image src={article.image} alt="" fill sizes="(max-width: 768px) 100vw, 50vw" className="object-cover transition duration-500 group-hover:scale-105"/></div><p className="mt-5 text-xs font-bold uppercase tracking-wider text-[var(--wine)]">{article.category} · {article.readTime}</p><h3 className="mt-2 text-2xl group-hover:text-[var(--wine)]">{article.title}</h3><p className="mt-3 leading-7 text-[var(--muted)]">{article.excerpt}</p></Link></article>}
