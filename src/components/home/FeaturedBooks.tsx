import {books} from "@/data/books";import {BookCard} from "@/components/books/BookCard";import {Container} from "@/components/layout/Container";import {SectionHeading} from "@/components/ui/SectionHeading";
export function FeaturedBooks(){return <section className="py-24"><Container><SectionHeading eyebrow="Biblioteca Ciocârlia" title="Două volume. O călătorie de la semn la sens." intro="Cărți pentru cei care iubesc ia, dar vor să privească dincolo de frumusețea ei."/><div className="mt-12 grid gap-7">{books.map(b=><BookCard key={b.id} book={b}/>)}</div></Container></section>}
