import {testimonials} from "@/data/testimonials";import {Container} from "@/components/layout/Container";import {SectionHeading} from "@/components/ui/SectionHeading";
export function TestimonialsSection(){return <section className="py-24"><Container><SectionHeading eyebrow="Din comunitate" title="Povești care merg mai departe."/><div className="mt-12 grid gap-5 md:grid-cols-2">{testimonials.map(t=><blockquote className="rounded-3xl bg-[var(--paper)] p-7" key={t.name}><p className="font-serif text-2xl leading-9">„{t.quote}”</p><footer className="mt-5 text-sm text-[var(--muted)]">{t.name} · {t.place}</footer></blockquote>)}</div></Container></section>}
