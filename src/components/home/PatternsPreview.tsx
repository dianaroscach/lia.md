import {patterns} from "@/data/patterns";import {PatternCard} from "@/components/patterns/PatternCard";import {Container} from "@/components/layout/Container";import {SectionHeading} from "@/components/ui/SectionHeading";import {Button} from "@/components/ui/Button";
export function PatternsPreview(){return <section className="bg-[#ede1ce] py-24"><Container><div className="flex flex-wrap items-end justify-between gap-6"><SectionHeading eyebrow="Atelier" title="Planșe pentru mâini răbdătoare."/><Button href="/planse" variant="secondary">Vezi toate planșele</Button></div><div className="mt-12 grid gap-6 md:grid-cols-3">{patterns.slice(0,3).map(p=><PatternCard key={p.id} pattern={p}/>)}</div></Container></section>}
