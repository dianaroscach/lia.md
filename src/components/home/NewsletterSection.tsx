"use client";import {FormEvent,useState} from "react";import {Container} from "@/components/layout/Container";import {Input} from "@/components/ui/Input";import {Button} from "@/components/ui/Button";import {trackEvent} from "@/lib/analytics";
export function NewsletterSection(){const [done,setDone]=useState(false);return <section className="border-b border-[var(--line)] bg-[var(--cream)] py-9"><Container className="grid items-center gap-6 md:grid-cols-[.75fr_1.25fr]"><div><h2 className="text-2xl">Primește scrisori din atelier</h2><p className="mt-2 font-serif text-xs text-[var(--muted)]">Noutăți despre cărți, planșe și povești cusute.</p></div><div><form className="flex flex-col gap-2 sm:flex-row" onSubmit={(e:FormEvent)=>{e.preventDefault();setDone(true);trackEvent("newsletter_signup")}}><Input type="email" required aria-label="Adresa de email" placeholder="Adresa ta de email"/><Button className="shrink-0 sm:min-w-36">Mă înscriu</Button></form>{done&&<p className="mt-2 text-xs text-[var(--olive)]" role="status">Mulțumim! Înscrierea demonstrativă a fost înregistrată.</p>}</div></Container></section>}
